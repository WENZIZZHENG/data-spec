## 1. OpenSpec

- [x] 1.1 新增 P3-1 proposal/design/tasks/spec delta。
- [x] 1.2 运行 `openspec validate add-reverse-sql-import --strict`。

## 2. 后端

- [x] 2.1 增加反向导入预览模型、Service 和 Controller。
- [x] 2.2 增加单元测试覆盖字段候选、缺注释和别名命中。

## 3. 前端

- [x] 3.1 增加反向导入 API 封装与类型。
- [x] 3.2 新增反向导入页面。
- [x] 3.3 侧边栏增加反向导入入口。

## 4. 验证与提交

- [x] 4.1 更新 TODO.md P3-1 状态。
- [x] 4.2 运行 `mvn test`。
- [x] 4.3 运行 `pnpm build`。
- [x] 4.4 运行 OpenSpec validate 和 diff 空白检查。
- [x] 4.5 直接代码评审，不使用子 agent。
- [x] 4.6 创建本地 commit。
