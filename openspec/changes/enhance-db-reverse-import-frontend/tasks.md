## 1. 状态与测试基线

- [x] 1.1 抽出数据库直连导入的表选择和候选字段选择工具函数，并先补 Node 单测覆盖搜索、全选、候选 key 和提交过滤。
- [x] 1.2 将工具函数接入 `ReverseImport.vue`，保持现有 API wrapper 和类型不变。

## 2. 前端流程改造

- [x] 2.1 在数据库直连模式增加步骤条、连接状态、表选择工具栏和已选数量反馈。
- [x] 2.2 将预览区按表分组展示候选字段，支持候选字段勾选和全选/清空候选。
- [x] 2.3 确认导入前增加确认弹窗，导入完成后展示新增/跳过列表和跳转字段库入口。

## 3. 验证与收尾

- [x] 3.1 运行新增 Node 单测和 `pnpm build`。
- [x] 3.2 运行 `openspec validate enhance-db-reverse-import-frontend`，同步 TODO 状态和 README/文档（如实际能力说明需要）。
- [x] 3.3 进行直接代码评审（不使用子 agent），修复发现的问题后提交本地 commit。
