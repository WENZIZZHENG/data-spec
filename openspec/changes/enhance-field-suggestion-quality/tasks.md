## 1. 测试基线

- [x] 1.1 新增字段推荐单元测试，覆盖中文同义词、英文别名和拼音缩写命中。
- [x] 1.2 新增泛化词降权测试，确保具体语义字段排在宽泛字段前。
- [x] 1.3 新增敏感字段命中原因测试。
- [x] 1.4 新增 fallback 标准字段名测试，覆盖 `user_id`、`mobile_no`、`amount_cent`。

## 2. 后端推荐质量实现

- [x] 2.1 在 `FieldServiceImpl` 中增加轻量语义词库和 query/field 语义归一逻辑。
- [x] 2.2 将语义组命中接入现有评分模型，并保留现有 exact/contains/token 匹配兼容行为。
- [x] 2.3 对泛化词-only 匹配降权，避免宽泛字段误排第一。
- [x] 2.4 在敏感字段推荐的 `matchReason` 中追加敏感提示。
- [x] 2.5 增强 fallback 字段名生成，优先输出 canonical snake_case 候选。

## 3. 文档与验证

- [x] 3.1 更新 README/TODO，说明 P5-6 第一版完成范围和后续边界。
- [x] 3.2 运行目标后端测试并观察新增测试先失败后通过。
- [x] 3.3 运行后端全量测试、OpenSpec validate 和 diff 检查。
- [x] 3.4 进行直接代码评审（不使用子 agent），修复发现的问题。
- [x] 3.5 创建本地 commit，并准备进入下一个待办。
