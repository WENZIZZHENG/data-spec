# DataSpec P6 候选价值评审

更新时间：2026-07-12

本文件用于记录剩余 P6 候选哪些值得继续做、哪些应合并、哪些可以删除或归档为不做。评审以个人 / 小团队快速使用为口径，优先保留能让 AI 更稳定理解、生成、导入、校验和维护数据字段标准的任务。

## 评审状态

- 已接入独立只读评审子 agent：`019f4736-840b-71b3-bf1a-4969636fe805`。
- 子 agent 用途：只读评审剩余 P6 候选池和相关代码表面，判断保留、合并、删除和暂缓。
- 子 agent 生命周期：已完成并关闭；未修改文件、未 commit、未运行重型测试。
- 主 agent 操作：已把根 TODO.md 瘦身为入口，将 127 个已完成 P5/P6 条目归档到 docs/archive/todo-completed-p5-p6.md，将 61 个未完成候选保留在 docs/todo-p6-candidates.md。

## 结论摘要

- 当前不建议按编号继续线性顺扫 63 个候选。
- 下一轮优先做“前端可访问性”以提升人工效率和 browser automation 稳定性；测试数据包与消费端兼容已进入实现收口。
- 大量候选应合并成主题包处理；独立做会造成平台化膨胀和重复实现。
- 若一个候选已被现有 API、schema、README 或 OpenSpec specs 覆盖，优先归档完成或删除独立候选，不再保留为待办。

## 高价值保留

建议按下面顺序选下一项，不要求一次性全做：

1. `P6-84` 前端可访问性与键盘操作基线：提升人工效率，也让 browser automation 更稳。

## 已完成高价值项

- `P6-153` AI Context 注入防护：已随 `add-ai-context-safety-controls` 完成第一版。
- `P6-158` 字段可见性等级：已作为 `add-ai-context-safety-controls` 的字段级安全决策完成第一版。
- `P6-165` 标准对象稳定标识：已随 `add-stable-standard-refs-and-ai-output-checks` 完成第一版。
- `P6-166` AI 输出后置校验：已随 `add-stable-standard-refs-and-ai-output-checks` 完成第一版。
- `P6-167` 标准查询 DSL：已随 `add-standard-query-dsl` 完成第一版。
- `P6-76` 业务对象关系图与表模板依赖：已随 `add-business-object-table-standards` 完成第一版。
- `P6-106` 表级约束、索引与主外键标准：已随 `add-business-object-table-standards` 完成第一版。
- `P6-77` 派生字段、单位换算与口径规则：已随 `add-field-semantics-knowledge-cards` 完成第一版。
- `P6-107` 代码集与枚举值生命周期：已随 `add-field-semantics-knowledge-cards` 完成第一版。
- `P6-161` AI 可读字段知识卡片：已随 `add-field-semantics-knowledge-cards` 完成第一版。
- `P6-175` 指标口径与字段标准映射层：已随 `add-field-semantics-knowledge-cards` 完成第一版。
- `P6-184` 字段标准中英文命名与翻译辅助：已随 `add-field-semantics-knowledge-cards` 完成第一版。
- `P6-185` 标准驱动测试数据与边界用例包：已随 `add-standard-test-data-compat-suite` 完成实现、验证、独立评审和 commit 收口。
- `P6-176` 标准消费端兼容验收套件：已随 `add-standard-test-data-compat-suite` 完成实现、验证、独立评审和 commit 收口。

## 合并处理

- 标准语义建模：`P6-76 + P6-106` 已合并为“业务对象与表结构标准闭环”并完成第一版。
- 字段语义规则：`P6-77 + P6-107 + P6-161 + P6-175 + P6-184` 已合并为“字段语义、枚举、口径与命名知识卡”并完成第一版。
- AI Context 安全：`P6-153 + P6-158 + P6-164` 已完成第一版，后续只在发现新泄漏面时补安全增强。
- 变更预演与迁移：合并 `P6-74 + P6-80 + P6-98 + P6-117 + P6-139 + P6-160 + P6-108`。
- 候选采纳管道：合并 `P6-110 + P6-111 + P6-112 + P6-113 + P6-136 + P6-142`。
- 运行诊断与漂移：合并 `P6-101 + P6-103 + P6-119 + P6-149`。
- 前端稳定性：合并 `P6-84 + P6-86 + P6-116 + P6-122 + P6-151`。
- 契约消费：合并 `P6-123 + P6-154 + P6-156 + P6-176`。

## 已处理：删除或归档独立候选

本节条目已按评审结论处理，不再保留在候选池：6 项归档为现有能力覆盖，9 项移入删除 / 不做归档。

- P6-114：已归档为现有能力覆盖。
- P6-115：已归档为现有能力覆盖。
- P6-148：已归档为现有能力覆盖。
- P6-170：已归档为现有能力覆盖。
- P6-97：已归档为现有能力覆盖。
- P6-94：已归档为现有能力覆盖，后续仅作为二期增强来源。
- P6-99：已移入删除 / 不做归档。
- P6-100：已移入删除 / 不做归档。
- P6-105：已移入删除 / 不做归档。
- P6-125：已移入删除 / 不做归档，价值并入前端稳定性主题。
- P6-128：已移入删除 / 不做归档，价值并入前端性能主题。
- P6-132：已移入删除 / 不做归档。
- P6-147：已移入删除 / 不做归档。
- P6-169：已移入删除 / 不做归档。
- P6-172：已移入删除 / 不做归档。

## 暂缓观察

- `P6-95`：自定义规则 SDK；等真实项目规则频繁超出内置规则再做。
- `P6-96`：语义检索；等字段数明显变大且术语表 / 别名不够用再做。
- `P6-102`、`P6-163`：连接器 registry/profile；等新增第三种以上数据源再推进。
- `P6-131`：真实样例采样涉及隐私；需先完成可见性与安全红线。
- `P6-138`：Secret Provider 会触碰凭据边界；除非重复输入凭据成为主要痛点。
- `P6-157`：离线分发包；等换机或断网启动成为真实需求。
- `P6-168`：MCP 分页；等 resource 真正触达客户端上下文限制。
- `P6-171`：dbt / Great Expectations 导出；等业务仓库明确使用这些测试体系。
- `P6-173`：编辑器 Code Action；等 CLI / Problem Matcher 不够用再做。
- `P6-174`：跨协议导出；等 Protobuf、Avro 或 JSON Schema 有明确消费方。

## 后续清理动作

1. 已完成第一轮删除 / 归档、高价值交付归档、P6-76/P6-106 主题包归档、字段语义主题包归档和 P6-185/P6-176 合并实现：候选池从 91 项降为 61 项。
2. 下一步按“合并处理”把候选池继续压缩成 8-12 个主题包，减少后续 agent 读取负担。
3. 未来新增想法先落到已有主题包，不再机械追加 P6-189。
4. 下一个开发任务建议从 `P6-84` 前端可访问性与键盘操作基线开始。
5. 剩余待办和时间估算见 docs/todo-p6-remaining-estimates.md。
