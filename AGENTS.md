# AGENTS.md 项目级完整配置

本文件是 data-spec 的完整可执行项目规范，不依赖个人全局配置。运行环境如有平台级默认规则，只能作为不冲突的补充，不得新增本文件未声明的门禁。

规则优先级：用户最新指令 > 本项目 `AGENTS.md` / `SDD.md` > 运行环境平台规则。

## 0. 语言与输出

- 始终使用中文回复；技术名词、代码名、参数名可保留 English。
- 人读字段如 `task_title`、`description`、`commit_message` 使用中文。
- 回复清晰直接，先给解决方案，再按任务需要展开必要原因和细节。
- 最终回复只保留适用项：改了什么、如何验证、是否 commit、剩余风险或下一步；不适用项可省略。

## 1. 项目目标

- data-spec 当前优先服务个人 / 小团队快速使用，核心目标是让 AI 能稳定理解、生成、导入、校验和维护数据字段标准。
- 默认采用“最小可用闭环优先”：先完成能真实使用的一条链路，再补治理、审批、权限和批量平台化能力。
- 保留质量底线：最小改动、遵循既有模式、必要验证、必要注释、UTF-8、非破坏性 Git。

## 2. 通用工作方式

- 默认最小改动，只修改完成任务必需的代码和文档。
- 修改前先定位相关文件，不做无目的全仓库扫描。
- 用户明确要求实现、修复或修改时直接执行；只有关键信息缺失且无法合理假设时才提问。
- 用户明确只要求建议、评审、解释或方案时，先保持只读，不主动改文件。
- 小任务按“定位问题 -> 最小改动 -> 必要验证 -> 说明结果”处理。
- 常规开发命令、测试、构建、文件编辑和本地 commit 可直接执行，不重复向用户索要常规权限。
- 不主动 `push`，除非用户明确要求。

## 3. 两段式流程判断

先判断是否进入 SDD；进入 SDD 后，再按 `SDD.md` 判断 lite / standard / full。

必须进入 SDD 的硬触发：

- 改数据库 schema、迁移、索引、字段模型、导入写入规则或历史数据兼容。
- 改 API route、OpenAPI schema、请求 / 响应字段、错误码、分页结构或跨端契约。
- 改持久化写入、事务、幂等、并发、可恢复性、数据一致性。
- 改敏感信息、数据库连接凭据、认证、权限、安全边界。
- 改 CLI / MCP / AI 外部协议的入参、出参、可被第三方依赖的行为。
- 改架构、构建、部署、任务调度或核心解析 / 校验链路。
- 用户明确要求按 SDD、OpenSpec、规范项目或设计文档流程执行。

默认不进入 SDD：

- 普通问答、只读分析、轻量 prompt 文案优化。
- README、TODO、说明文案、示例、小样式、小型页面体验修正。
- 不改变契约和存储结构的前端展示、薄 API 调用、类型引用修正。
- 单点 bug、测试补丁、配置微调。

快速修规则：

- 用户要求“快速修”不能覆盖上面的硬触发。
- 命中安全、存储、公共契约、CLI / MCP / AI 协议时，只能把流程降为 SDD lite / hotfix；除非用户明确要求跳过 SDD，并在最终回复记录风险。

data-spec 示例：

- SQL 检查记录前端展示：标准模式。
- 数据库连接配置 UI 的只读展示：标准模式。
- 数据库直连反向导入并写入数标：进入 SDD。
- 修改 MCP tool 入参 / 出参 schema：进入 SDD。
- README 能力描述修正：快速模式。

## 4. 三类非 SDD / SDD 触发模式

### 快速模式

适用：文档、小文案、小样式、小前端展示、小 API 薄封装、单点测试补丁，且不命中 SDD 硬触发。

工作方式：

- 不强制 OpenSpec。
- 不强制独立代码评审子 agent。
- 运行最相关验证；纯文档可人工检查加 `git diff --check`。
- 纯文档小改可不 commit；需要保留历史时可单独 commit。

### 标准模式

适用：普通产品功能，影响范围清晰、可回退，不改变长期公共契约或存储结构。

工作方式：

- 默认不强制 OpenSpec；使用 TODO、轻量任务卡或最终回复记录范围、验收点和验证结果。
- 必须结构化自审：需求覆盖、边界、错误处理、类型、测试、文档和无关改动。
- 独立 agent 评审可用时优先使用；工具不可用不阻断。
- 运行相关模块验证，例如 `mvn test`、`pnpm test`、`pnpm build`、`node --test tools/*.test.mjs` 中与改动匹配的命令。

### SDD 触发模式

适用：命中第 3 节硬触发，或用户明确要求 SDD / OpenSpec。

工作方式：

- 必须进入 `SDD.md`，由 `SDD.md` 决定 lite / standard / full。
- 必须完成与风险匹配的 artifacts、测试、文档和验证证据。
- commit 或 OpenSpec archive 前必须满足对应评审门禁。

## 5. 代码注释与实现规范

- 新代码必须遵循项目现有目录结构、命名风格、错误处理方式、日志方式、测试组织方式和相似实现。
- 注释解释“为什么这样做”“业务约束是什么”“边界和兼容逻辑是什么”，不要重复描述代码已经清楚表达的“做了什么”。
- 复杂业务规则、非显而易见的状态流转、边界条件、兼容逻辑、异常兜底、降级 / 重试策略、公共函数、导出 API、配置项、数据结构约束和跨模块契约，必须补充必要注释或文档说明。
- 与安全、权限、数据一致性、幂等性、事务、并发、存储写入、用户可见结果相关的关键逻辑，必须说明关键约束或设计取舍。
- 简单赋值、直观变量、常规框架样板代码不强制注释；若需要注释才能读懂，应优先重命名、拆分函数或收窄作用域。
- 不为假设中的未来需求增加框架式抽象；只有当前需求真实需要，或同类模式已经稳定出现，才新增抽象。

## 6. 编辑与验证

- 修改代码或配置优先使用编辑工具或 patch 工具，避免命令行批量文本替换。
- 多文件可并行分析；同一文件修改保持顺序和精确。
- 禁止为小改动删除大段代码后重写。
- 文件统一使用 UTF-8；新增代码注释使用中文。
- 读取 Markdown、YAML、JSON、CSV 时优先显式使用 UTF-8。
- 新增测试、契约测试或质量门禁时，必须接入项目已有验证入口，避免只手动运行一次而无法防回归。

验证分层：

- 快速模式：最相关验证；纯文档可人工检查加 `git diff --check`。
- 标准模式：受影响模块的测试或构建。
- SDD lite / standard：相关模块验证 + `openspec validate <change-id> --strict`。
- SDD full：完整或接近完整验证，常用组合包括 `mvn test`、`pnpm test`、`pnpm build`、`node --test tools/*.test.mjs`、`openspec validate --all`、`git diff --check`。
- 无法运行验证时必须说明原因、替代检查和建议后续命令。

## 7. 评审门禁

| 场景 | commit / archive 前要求 |
| --- | --- |
| 快速模式 | 自查即可 |
| 标准模式 | 结构化自审；agent 可用时优先使用 |
| SDD lite / standard | agent 优先；工具不可用时可结构化自审并记录原因 |
| SDD full | 必须独立 agent 评审 |
| 安全、存储、OpenAPI 契约、CLI / MCP / AI 外部协议 | 必须独立 agent 评审 |

评审 findings 必须修复，或用技术理由明确记录暂不处理的原因。

## 8. 文档更新节奏

- 只更新本次变更直接影响且已存在的权威文档。
- 用户可见能力、启动方式、命令、接口或核心边界变化时更新 `README.md`。
- 待办、优先级或状态变化时更新 `TODO.md`。
- 长期取舍、方案原因或后续影响需要沉淀时更新 `DECISIONS.md` 或项目已有等价文档；不存在时不强行创建。
- OpenSpec artifacts 是 SDD 变更的主记录；快速和标准模式不为了流程完整性强行创建 OpenSpec。

## 9. Git 提交规则

- 只读、建议、方案和评审任务不 commit。
- 完成修改并通过必要验证，且满足对应 OpenSpec 与评审门禁后，默认创建本地 commit。
- 仓库状态安全才 commit。安全 = 仅本次任务相关文件有变更，或可精确 stage 本次变更；同文件混有无法拆分的用户改动则不 commit。
- commit 只 stage 本次任务产生的变更。
- commit message 使用中文，概括用户可理解的变更结果。
- 不主动 push。

## 10. 工具与权限

- 常见开发命令可直接执行。
- 涉及系统文件、注册表、批量删除、破坏性 Git 操作时先获得用户明确确认。
- 破坏性操作包括但不限于 `git reset --hard`、强制覆盖、递归删除、系统级修改；除非用户明确要求，不主动执行。
- 非必要不联网；涉及最新版本、价格、法规、新闻、依赖文档等易变化信息时再 web verify。
- 按任务需要使用相关 skills / plugins；用户点名的能力优先加载。
